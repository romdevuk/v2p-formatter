main: The script which will convert video(mp4) to image(jpg) sequence and have the following 

output options:
save in folder with image Id 1,2,3…;
Create PDF file with images id 1,2,3,4;


User interface: choose mp4 to upload. Pick time frames which will be saved as photos;

Based on above ask me further questions in spec md file to answer,
Once answered create the final tech spec for approval;
