‘Observation Media’ module

A module where user will see a photo/videos(media) list thumbs from specific folder and assign it with relevant placeholder;

The placeholder is a table with 2 columns; each column to have 1 media max; if video = filename only;

Placeholder names: text added by user will have placeholders already{{Site_Arrival_Induction}}

Placeholder table format: a table. 2 columns; 1 cell is 1 media max; if a video - just a  file name.
User can drag media in a table so other media organised accordingly; 

Text: user to copy paste text in a working area; the text will come with placeholder names already;

UI(create text wireframe for approval): left column = media(user selects media/toggle and assigns it to specific placeholder name; user to have an option to reshuffle media order in a final table); 
right column: text and tables with relevant media shown;

Actions: save in DOCX
